package model;

import java.util.logging.Logger;

public class CollideExceptions extends Exception {
    private static final Logger LOGGER = Logger.getLogger(String.valueOf(CollideExceptions.class));
}
